{
  "name": "dsh-annotation-patched replay manifest",
  "targetFile": "client.js",
  "base": {
    "repo": "omdsh-dev/dsh-annotation",
    "baseVersion": "1.4.11-preview.1",
    "baseCommit": "ab594842",
    "note": "干净基座：gh api repos/omdsh-dev/dsh-annotation/contents/client.js?ref=<commit>（--fetch 自动化）。本轮因宿主 DSH 0.1.6-alpha.2 把 composer 换成 Lexical contenteditable 而从 fd24ef92（1.4.1 + issue#20 fix，2026-09-04 前基座）迁到 v1.4.11-preview.1（适配 DSH 0.1.6-alpha.1）。上游同期把包名改为 @changfenhuang/dsh-annotation。更早历史基座 v1.3.13=a37edb01"
  },
  "terminology": [
    {
      "from": "批注",
      "to": "引用",
      "note": "全局术语改名：UI 文案与注释（英文 Annotation N 协议串不动）"
    }
  ],
  "opsCount": 46,
  "verifiedAgainst": "2026-09-18 DSH 0.1.6-alpha.2：i18n 补齐；ab594842 基座全量重放字节级可复现，npm test 全绿",
  "ops": [
    {
      "id": "op00-pkg-id",
      "group": "pkg-id",
      "description": "fork 包名：@omdsh-dev/dsh-annotation → @dsh-external/dsh-annotation-patched（bundle 头部 id）",
      "find": "window.__ModuleLoader__.load({\n  // 必须与 package.json \"name\" 完全一致，否则 client-modules 报：\n  // bundle loaded without registering \"@changfenhuang/dsh-annotation\"\n  id: '@changfenhuang/dsh-annotation',\n  factory: (require) => {\n    'use strict'\n",
      "replace": "window.__ModuleLoader__.load({\n  // 必须与 package.json \"name\" 完全一致，否则 client-modules 报：\n  // bundle loaded without registering \"@dsh-external/dsh-annotation-patched\"\n  id: '@dsh-external/dsh-annotation-patched',\n  factory: (require) => {\n    'use strict'\n",
      "adaptedFor": "ab594842：上游 v1.4.11 把包名从 @omdsh-dev/dsh-annotation 改为 @changfenhuang/dsh-annotation（loader id 与报错注释同步），find 的域名前缀随之改锚；replace 仍是 fork 包名 @dsh-external/dsh-annotation-patched。"
    },
    {
      "id": "op01-p0814e-marker-zindex",
      "group": "p0814e-marker-zindex",
      "description": "PATCH(2026-08-14e)：高亮/编号层 z-index 恢复 900/940，配合区域过滤方案（注释说明根因）",
      "find": "        '.dsh-ann-error { color: var(--dsw-alias-state-error-primary, #ff7a7a);',\n        '  font-size: 12px; margin-top: 8px; word-break: break-word; }',\n        '.dsh-ann-hl { position: fixed; z-index: 900; background: rgba(255, 195, 0, .15);',\n        '  border-radius: 2px; pointer-events: none; animation: dsh-ann-fadein .15s ease; }',\n",
      "replace": "        '.dsh-ann-error { color: var(--dsw-alias-state-error-primary, #ff7a7a);',\n        '  font-size: 12px; margin-top: 8px; word-break: break-word; }',\n        // PATCH(2026-08-14e): 高亮/编号盖输入栏的根因是 buildMarkers 把滚动后\n        // 进入 composer 区域的 rect 也渲染成了 fixed 层——正解是「区域过滤」\n        // （buildMarkers 跳过与输入栏重叠的 rect），而非压 z-index（压太低会被\n        // 对话区元素盖住，chip 点不开、取消/删除失效）。z-index 恢复 900/940。\n        '.dsh-ann-hl { position: fixed; z-index: 900; background: rgba(255, 195, 0, .15);',\n        '  border-radius: 2px; pointer-events: none; animation: dsh-ann-fadein .15s ease; }',\n"
    },
    {
      "id": "op02-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：chipLayer 动态重定位——scroll/resize 监听 + React 替换 composer 后重取锚",
      "find": "      window.addEventListener('scroll', onLayoutChange, true)\n      window.addEventListener('resize', onLayoutChange)\n\n      function mutationRelevant(mutations) {\n",
      "replace": "      window.addEventListener('scroll', onLayoutChange, true)\n      window.addEventListener('resize', onLayoutChange)\n      // composer 可能是 fixed/sticky 层，滚动或窗口尺寸变化时其 viewport 坐标也可能变。\n      window.addEventListener('scroll', scheduleChipUpdate, true)\n      window.addEventListener('resize', scheduleChipUpdate)\n\n      function mutationRelevant(mutations) {\n"
    },
    {
      "id": "op03-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：chipLayer 动态重定位——scroll/resize 监听 + React 替换 composer 后重取锚（续 2/2）",
      "find": "      }\n      var observer = new MutationObserver(function (mutations) {\n        if (!mutationRelevant(mutations)) return\n        onLayoutChange()\n",
      "replace": "      }\n      var observer = new MutationObserver(function (mutations) {\n        // React 可能整体替换 composer；ResizeObserver 只绑定旧节点时，需在 DOM 变化\n        // 后重新取锚。仅在节点身份变化时调度，避免 chipLayer 自己的 DOM 更新形成循环。\n        if (ui.quotes.length > 0\n          && document.querySelector('[data-composer-card]') !== chipComposerCard) {\n          scheduleChipUpdate()\n        }\n        if (!mutationRelevant(mutations)) return\n        onLayoutChange()\n"
    },
    {
      "id": "op05-p0814c-single-button",
      "group": "p0814c-single-button",
      "description": "PATCH(2026-08-14c)：单按钮制——图标恒无；v1.4.1 适配：文案走 t() i18n",
      "find": "          bar.appendChild(ghostButton(\n            already ? null : ICONS.plus,\n            already ? t('actions.already') : t('actions.annotate'),\n            already ? t('actions.alreadyTitle') : t('actions.annotateTitle'),\n            already,\n            enterEditing,\n          ))\n",
      "replace": "          // PATCH(2026-08-14c): 单按钮制——图标恒无（v1.4.x i18n 适配：文案走\n          // t()，「可留空 = 仅引用原文」提示见 annotateTitle 字典补充）。\n          bar.appendChild(ghostButton(\n            null,\n            already ? t('actions.already') : t('actions.annotate'),\n            already ? t('actions.alreadyTitle') : t('actions.annotateTitle'),\n            already,\n            enterEditing,\n          ))\n",
      "adaptedFor": "fd24ef92：上游 1.4.0 引入 locale i18n，原硬编码文案锚点失效"
    },
    {
      "id": "op06-p0814c-single-button",
      "group": "p0814c-single-button",
      "description": "PATCH(2026-08-14c)：zh 字典补「可留空 = 仅引用原文」提示（单按钮语义）",
      "find": "          annotateTitle: '为选中的内容写一条引用',\n",
      "replace": "          annotateTitle: '为选中的内容写一条引用（可留空 = 仅引用原文）',\n"
    },
    {
      "id": "op07-p0814c-single-button",
      "group": "p0814c-single-button",
      "description": "PATCH(2026-08-14c)：en 字典补 leave-empty 提示（单按钮语义）",
      "find": "          annotateTitle: 'Write a note about the selected text',\n",
      "replace": "          annotateTitle: 'Write a note about the selected text (leave empty to quote without a note)',\n"
    },
    {
      "id": "op08-p0814e-delete-in-edit",
      "group": "p0814e-delete-in-edit",
      "description": "PATCH(2026-08-14e)：编辑态提供「删除引用」按钮",
      "find": "          row.appendChild(cancel)\n          row.appendChild(save)\n          card.appendChild(row)\n          if (ui.error !== null) {\n",
      "replace": "          row.appendChild(cancel)\n          row.appendChild(save)\n          // PATCH(2026-08-14e): 编辑已有引用时提供「删除引用」（v2 加回）——删除入口\n          // 平时在「输入框旁标签 → 悬浮面板」里，编辑态直接删更顺手。\n          if (ui.editingId !== null) {\n            var del = document.createElement('button')\n            del.type = 'button'\n            del.className = 'dsh-ann-cancel'\n            del.style.cssText = 'color:#ff8a8a;border-color:rgba(255,107,107,.4);'\n            del.textContent = '删除引用'\n            del.addEventListener('click', function () {\n              removeQuote(ui.editingId)\n              closeToolbar()\n            })\n            row.appendChild(del)\n          }\n          card.appendChild(row)\n          if (ui.error !== null) {\n"
    },
    {
      "id": "op09-p0814e-region-filter",
      "group": "p0814e-region-filter",
      "description": "PATCH(2026-08-14e)：buildMarkers 区域过滤——高亮/编号不落入 composer 区域；滚出可视区不显示编号",
      "find": "      function buildMarkers() {\n        if (ui.quotes.length === 0) return\n        var placed = []\n        for (var i = 0; i < ui.quotes.length; i++) {\n",
      "replace": "      function buildMarkers() {\n        if (ui.quotes.length === 0) return\n        // PATCH(2026-08-14e): 区域过滤——高亮/编号只渲染在对话可视区（composer 卡片\n        // 顶部之上）。滚动后引用文本滚进输入栏区域时其 rect.top ≥ composerTop，\n        // 跳过即可，fixed 层永远不落到输入栏上（z-index 高位也不会盖输入栏）。\n        var composerTop = window.innerHeight\n        var composerCard = document.querySelector('[data-composer-card]')\n        if (composerCard !== null) {\n          var cr = composerCard.getBoundingClientRect()\n          if (cr.top > 0) composerTop = cr.top\n        }\n        var placed = []\n        for (var i = 0; i < ui.quotes.length; i++) {\n"
    },
    {
      "id": "op10-p0814e-region-filter",
      "group": "p0814e-region-filter",
      "description": "PATCH(2026-08-14e)：buildMarkers 区域过滤——高亮/编号不落入 composer 区域；滚出可视区不显示编号（续 2/3）",
      "find": "            var rect = rects[c]\n            if (rect.width === 0 || rect.height === 0) continue\n            var hl = document.createElement('div')\n            hl.className = 'dsh-ann-hl'\n",
      "replace": "            var rect = rects[c]\n            if (rect.width === 0 || rect.height === 0) continue\n            if (rect.top >= composerTop) continue\n            var hl = document.createElement('div')\n            hl.className = 'dsh-ann-hl'\n"
    },
    {
      "id": "op11-p0814e-region-filter",
      "group": "p0814e-region-filter",
      "description": "PATCH(2026-08-14e)：buildMarkers 区域过滤——高亮/编号不落入 composer 区域；滚出可视区不显示编号（续 3/3）",
      "find": "            var r = rects[k]\n            if (r.width === 0 || r.height === 0) continue\n            if (r.top > -8 && r.top < window.innerHeight) { anchor = r; break }\n          }\n          if (anchor === null && rects.length > 0 && rects[0].width > 0) anchor = rects[0]\n          if (anchor !== null) {\n            var chip = document.createElement('div')\n",
      "replace": "            var r = rects[k]\n            if (r.width === 0 || r.height === 0) continue\n            if (r.top > -8 && r.top < composerTop) { anchor = r; break }\n          }\n          // 引用文本整体滚出对话可视区（anchor 为 null）时不显示编号——\n          // 不 fallback 到 rects[0]，否则编号会落到输入栏区域。\n          if (anchor === null) continue\n          if (anchor !== null) {\n            var chip = document.createElement('div')\n"
    },
    {
      "id": "op12-p0814c-strip-confirm",
      "group": "p0814c-strip-confirm",
      "description": "PATCH(2026-08-14c)：插入 stripOldBlock（双语哨兵版）；v1.4.1 适配：复用上游 hasAnnotationBlock",
      "find": "      /** 提交前把引用块拼进 composer 草稿（随回车一起发送）。\n       *  返回 true 表示引用块已在草稿中（本次刚拼入，或之前已拼入未发送）。 */\n      function attachAndSend(e) {\n",
      "replace": "      /** 从草稿中剥离残留的旧引用块（\"我引用了以下…提问：\" / \"I annotated the\n       *  following…Ask:\" 整块），保留用户自己的输入。残留来源：上次 setDraft 后\n       *  提交失败/未触发，旧块留在草稿里而引用集已变——若不剥离，旧块会随下一条\n       *  消息重复发出且内容过期。PATCH(2026-08-14c): 竞态修复——剥离替代跳过；\n       *  全局替换一次剥净多个残留；双语哨兵适配 v1.4 i18n。非贪婪匹配到第一个\n       *  块尾标记：块拼在草稿最前，用户输入在其后，第一个标记必属块尾。 */\n      function stripOldBlock(draft) {\n        if (!hasAnnotationBlock(draft)) return draft\n        return draft.replace(\n          /\\n*(?:我引用了以下|I annotated the following)[\\s\\S]*?\\n\\n(?:提问：|Ask:)(?:\\n+)?/g,\n          '',\n        )\n      }\n\n      /** 提交前把引用块拼进 composer 草稿（随回车一起发送）。\n       *  返回 true 表示引用块已在草稿中（本次刚拼入，或之前已拼入未发送）。 */\n      function attachAndSend(e) {\n",
      "adaptedFor": "fd24ef92：JSDoc 与签名变化，正则升级双语哨兵"
    },
    {
      "id": "op13-p0814c-strip-confirm",
      "group": "p0814c-strip-confirm",
      "description": "PATCH(2026-08-14c)：有块即跳过 → 剥离重拼；v1.4.1 适配：锚点随上游 #20/i18n 更新",
      "find": "          // 草稿已含引用块（上次追加未发送）→ 不重复追加（zh/en 双哨兵）。\n          if (hasAnnotationBlock(draft)) {\n            annotationAttached = true\n            return true\n          }\n          var hasQuestion = draft.trim() !== ''\n          var block = buildBlock(hasQuestion)\n          shell.setDraft(block + (hasQuestion ? '\\n' + draft : ''))\n          annotationAttached = true\n",
      "replace": "          // PATCH(2026-08-14c): 残留块不跳过、剥离后重拼——上次追加未发送时草稿\n          // 里的旧块可能已过期（引用集已变），stripOldBlock 剥旧块再拼新块，\n          // 用户已输入文字保留在块后；发送确认制清理（watchInputDraft）不受影响。\n          var clean = stripOldBlock(draft)\n          // ab594842：上游 v1.4.11 纯引用块走 block.headOnly/formatOnly（无「提问：」\n          // 标记），不在 stripOldBlock 的标记正则内；按首尾文案补剥，保留块后用户文字。\n          if (clean === draft) {\n            clean = draft.replace(\n              /^\\n*(?:我引用了以下|I annotated the following)[\\s\\S]*?(?:请按「Annotation N：…」的格式，逐条回应以上引用。|Please respond to each annotation above in the format \"Annotation N: …\".)\\n*/,\n              '',\n            )\n            // 仍剥不动（未知格式）：整块作废，避免旧块随新块重复发出。\n            if (clean === draft) clean = ''\n          }\n          var hasQuestion = clean.trim() !== ''\n          var block = buildBlock(hasQuestion)\n          shell.setDraft(block + (hasQuestion ? '\\n' + clean : ''))\n          annotationAttached = true\n",
      "adaptedFor": "ab594842：上游 attachAndSend 改为 buildBlock(hasQuestion) 且把 annotationAttached/return 拆进 hasAnnotationBlock 分支；stripOldBlock 语义不变，按新变量名重写剥离后重拼（clean 非空才追加换行）。另补剥上游 v1.4.11 新增的无标记纯引用块（headOnly/formatOnly），否则该形态旧块会被当作用户文字重复拼入。"
    },
    {
      "id": "op14-p0814e-collapse-selection",
      "group": "p0814e-collapse-selection",
      "description": "PATCH(2026-08-14e)：关闭工具条时塌缩残留选区，修复取消/X 不生效",
      "find": "        if (ui.mode === 'closed') return\n        clearSettle()\n        ui.mode = 'closed'\n        ui.error = null\n",
      "replace": "        if (ui.mode === 'closed') return\n        clearSettle()\n        // PATCH(2026-08-14e): 塌缩残留选区——编辑已有引用时（chip 打开的编辑卡），\n        // 原文本选区在保存后仍存在；若不塌缩，selectionchange 的 settle 会在关闭\n        // 后 ~250ms 重新弹出工具条，表现为「取消/右上角 X 不生效」（新建引用时点\n        // 「引用」按钮已塌缩选区，故无此问题）。\n        try {\n          var sel = window.getSelection()\n          if (sel !== null && !sel.isCollapsed) sel.removeAllRanges()\n        } catch (_) { /* 选区操作失败忽略 */ }\n        ui.mode = 'closed'\n        ui.error = null\n"
    },
    {
      "id": "op15-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：updateChip 改造——ResizeObserver/rAF 合帧/动态测高定位",
      "find": "      document.body.appendChild(tipLayer)\n      var observedComposer = null\n      var composerObserver = typeof ResizeObserver === 'function'\n        ? new ResizeObserver(onLayoutChange)\n        : null\n\n      function updateChip() {\n        if (ui.quotes.length === 0) {\n          chipLayer.style.display = 'none'\n",
      "replace": "      document.body.appendChild(tipLayer)\n\n      // PATCH(2026-08-17): chipLayer 是 fixed 层，不能只在「引用数量变化」时定位一次。\n      // DSH composer 的 textarea 多行增长时，composer card 会从底部向上变高；若不重新\n      // 计算 card.top，标签就留在旧坐标，随后被输入框外壳「吞」进去并遮挡第一行内容。\n      // ResizeObserver 负责捕获自动增高，input/DOM 监听负责覆盖无 ResizeObserver 或\n      // composer 被 React 替换的情况；所有更新都合并到下一帧，避免输入时反复重排。\n      var chipComposerCard = null\n      var chipResizeObserver = null\n      var chipPositionFrame = null\n\n      function scheduleChipUpdate() {\n        if (chipPositionFrame !== null) return\n        chipPositionFrame = requestAnimationFrame(function () {\n          chipPositionFrame = null\n          updateChip()\n        })\n      }\n\n      function observeChipComposer(card) {\n        if (card === chipComposerCard) return\n        if (chipResizeObserver !== null) {\n          chipResizeObserver.disconnect()\n          chipResizeObserver = null\n        }\n        chipComposerCard = card\n        if (card !== null && typeof ResizeObserver === 'function') {\n          chipResizeObserver = new ResizeObserver(function () {\n            scheduleChipUpdate()\n          })\n          chipResizeObserver.observe(card)\n          // 某些布局只改变输入滚动区/textarea 的尺寸，外层 card 的 ResizeObserver\n          // 可能收不到变化；一并观察这两个节点，兼容不同 DSH snapshot。\n          var inputScroll = card.querySelector('[data-input-scroll]')\n          if (inputScroll !== null) chipResizeObserver.observe(inputScroll)\n          var textarea = card.querySelector('textarea')\n          if (textarea !== null) chipResizeObserver.observe(textarea)\n        }\n      }\n\n      function onComposerInput(e) {\n        var target = e.target\n        if (!(target instanceof Element) || target.closest('[data-composer-card]') === null) return\n        scheduleChipUpdate()\n      }\n      document.addEventListener('input', onComposerInput, true)\n\n      function updateChip() {\n        var card = document.querySelector('[data-composer-card]')\n        observeChipComposer(ui.quotes.length > 0 ? card : null)\n        if (ui.quotes.length === 0) {\n          chipLayer.style.display = 'none'\n",
      "adaptedFor": "ab594842：上游 updateChip 自带 observedComposer/composerObserver（只 observe card）；fork 的 observeChipComposer 额外 observe [data-input-scroll]+输入面并挂 input 事件，故 find 吞掉上游重复声明，replace 不恢复（避免双 ResizeObserver）。"
    },
    {
      "id": "op16-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：updateChip 改造——ResizeObserver/rAF 合帧/动态测高定位（续 2/3）；v1.4.1 适配：按 t(chip.count) 新上下文锚定",
      "find": "        chipLayer.appendChild(document.createTextNode(t('chip.count')))\n        var card = document.querySelector('[data-composer-card]')\n        if (card !== observedComposer) {\n          if (composerObserver !== null) composerObserver.disconnect()\n          observedComposer = card\n          if (composerObserver !== null && card !== null) composerObserver.observe(card)\n        }\n        if (card === null) { chipLayer.style.display = 'none'; return }\n",
      "replace": "        chipLayer.appendChild(document.createTextNode(t('chip.count')))\n        if (card === null) { chipLayer.style.display = 'none'; return }\n",
      "adaptedFor": "ab594842：上游 updateChip 内联了 observedComposer/composerObserver 观察块；op15 已在函数顶部取 card 交给 observeChipComposer，故 find 连带吞掉上游观察块与重复的 var card，replace 只留判空。"
    },
    {
      "id": "op17-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：updateChip 改造——ResizeObserver/rAF 合帧/动态测高定位（续 3/3）",
      "find": "        var r = card.getBoundingClientRect()\n        if (r.width === 0 || r.height === 0 || r.right <= 0 || r.bottom <= 0\n          || r.left >= window.innerWidth || r.top >= window.innerHeight) {\n          chipLayer.style.display = 'none'\n          return\n        }\n        var w = chipLayer.offsetWidth || 80\n        chipLayer.style.left = Math.max(8, r.right - w - 12) + 'px'\n        chipLayer.style.top = Math.max(8, r.top - 30) + 'px'\n        chipLayer.style.display = 'flex'\n      }\n",
      "replace": "        var r = card.getBoundingClientRect()\n        if (r.width === 0 || r.height === 0 || r.right <= 0 || r.bottom <= 0\n          || r.left >= window.innerWidth || r.top >= window.innerHeight) {\n          chipLayer.style.display = 'none'\n          return\n        }\n        var w = chipLayer.offsetWidth || 80\n        var h = chipLayer.offsetHeight || 22\n        chipLayer.style.left = Math.max(8, r.right - w - 12) + 'px'\n        chipLayer.style.top = Math.max(8, r.top - h - 8) + 'px'\n        chipLayer.style.display = 'flex'\n      }\n",
      "adaptedFor": "ab594842：上游 updateChip 新增视口可见性守卫（width/height/right/bottom/left/top 判定）；find 吞入该守卫，replace 保留并追加 fork 的动态测高定位（h = offsetHeight）。"
    },
    {
      "id": "op19-p0817-chip-reposition",
      "group": "p0817-chip-reposition",
      "description": "PATCH(2026-08-17)：chip 重定位相关监听/observer 的 dispose 清理",
      "find": "        window.removeEventListener('scroll', onLayoutChange, true)\n        window.removeEventListener('resize', onLayoutChange)\n        host.removeEventListener('pointerdown', onHostPointerDown)\n        observer.disconnect()\n",
      "replace": "        window.removeEventListener('scroll', onLayoutChange, true)\n        window.removeEventListener('resize', onLayoutChange)\n        window.removeEventListener('scroll', scheduleChipUpdate, true)\n        window.removeEventListener('resize', scheduleChipUpdate)\n        document.removeEventListener('input', onComposerInput, true)\n        if (chipPositionFrame !== null) {\n          cancelAnimationFrame(chipPositionFrame)\n          chipPositionFrame = null\n        }\n        if (chipResizeObserver !== null) {\n          chipResizeObserver.disconnect()\n          chipResizeObserver = null\n        }\n        chipComposerCard = null\n        host.removeEventListener('pointerdown', onHostPointerDown)\n        observer.disconnect()\n"
    },
    {
      "id": "op20-pkg-id",
      "group": "pkg-id",
      "description": "fork 包名：exports.name 同步为 fork 包名",
      "find": "    exports.name = '@changfenhuang/dsh-annotation'\n",
      "replace": "    exports.name = '@dsh-external/dsh-annotation-patched'\n",
      "adaptedFor": "ab594842：上游 exports.name 同步改名为 @changfenhuang/dsh-annotation，锚点域名随之更新（仍需单行锚，尾部新增 exports.inject）。"
    },
    {
      "id": "op21-flow-observer",
      "group": "p0827-flow-observer",
      "description": "补录 6c6dd18（2026-08-27）漏记改动：会话切换清空 pendingDeco / MutationObserver 收敛到消息流根节点 / 装饰轮询改有界 setTimeout（hunk 0）",
      "find": "\n        }\n        return false\n      }\n      var observer = new MutationObserver(function (mutations) {\n        // React 可能整体替换 composer；ResizeObserver 只绑定旧节点时，需在 DOM 变化\n        // 后重新取锚。仅在节点身份变化时调度，避免 chipLayer 自己的 DOM 更新形成循环。\n        if (ui.quotes.length > 0\n          && document.querySelector('[data-composer-card]') !== chipComposerCard) {\n          scheduleChipUpdate()\n        }\n        if (!mutationRelevant(mutations)) return\n        onLayoutChange()\n",
      "replace": "\n        }\n        return false\n      }\n      // 内容 observer 只绑定当前消息流，避免 document.body 上任意属性/浮层变化\n      // 都触发 O(全部消息行) 的 decorateAll。另用一个仅监听 childList 的轻量\n      // rootObserver 处理 SPA 会话切换时消息流/composer 根节点整体替换。\n      var observerTarget = null\n      var observer = new MutationObserver(function (mutations) {\n        if (!mutationRelevant(mutations)) return\n        onLayoutChange()\n        // 消息行插入/内容填充的瞬间同步执行气泡装饰（隐藏引用块 + 贴标签）：\n",
      "adaptedFor": "ab594842：上游 observer 回调尾部新增 hasRowInsert/scheduleAssistantDecorate 限流分支；本 op 只负责摘除 op03 注入的 chip 重定位块并引入 observerTarget，故 find 收缩到 onLayoutChange() 为止，上游限流分支原样保留在产物里。"
    },
    {
      "id": "op22-lexical-composer-editor",
      "group": "p0901-lexical-compat",
      "description": "PATCH(2026-09-01-lexical)：新增 composer 输入面判别 isComposerEditor()，同时认 <textarea>（旧宿主）与 Lexical contenteditable（新宿主）",
      "find": "        // 流式批次: 只做助手回复芯片的限流装饰(行内 data-streaming 守卫已保证\n        // 流式中不做替换, 结束后的首拍芯片滞后 ≤500ms), 不再逐批全文档扫描。\n        scheduleAssistantDecorate()\n      })\n      observer.observe(document.body, { childList: true, subtree: true, characterData: true, attributes: true })\n\n      function onKeyDown(e) {\n        if (e.key === 'Escape') {\n",
      "replace": "        // 流式批次: 只做助手回复芯片的限流装饰(行内 data-streaming 守卫已保证\n        // 流式中不做替换, 结束后的首拍芯片滞后 ≤500ms), 不再逐批全文档扫描。\n        scheduleAssistantDecorate()\n      })\n      function messageFlowRoot() {\n        return document.querySelector('[data-chat-flow]')\n          || document.querySelector('[data-focus-flow]')\n      }\n      function bindMessageObserver() {\n        var next = messageFlowRoot()\n        if (next === observerTarget) return\n        observer.disconnect()\n        observerTarget = next\n        if (next !== null) {\n          observer.observe(next, { childList: true, subtree: true, characterData: true })\n        }\n      }\n      var rootObserver = new MutationObserver(function () {\n        bindMessageObserver()\n        // React 可能整体替换 composer；ResizeObserver 只绑定旧节点时，需重新取锚。\n        if (ui.quotes.length > 0\n          && document.querySelector('[data-composer-card]') !== chipComposerCard) {\n          scheduleChipUpdate()\n        }\n      })\n      rootObserver.observe(document.body, { childList: true, subtree: true })\n      bindMessageObserver()\n\n      // ---------- composer 输入面判别（textarea / Lexical contenteditable） ----------\n      // PATCH(2026-09-01-lexical): 宿主 composer 从 <textarea> 换成 Lexical\n      // contenteditable（ComposerContentEditable），判别必须同时认两种形态。\n      // 旧 DSH snapshot 的 composer 是 <textarea>；新 snapshot（Lexical 化的\n      // ComposerContentEditable）是 <div contenteditable=\"true\">。只认 textarea 会让\n      // 回车拼稿守卫永不成立：引用块不随消息发出，引用集也不被清空（chip 常驻）。\n      /** @param {EventTarget|null} el @returns {boolean} 事件目标是否为 composer 输入面 */\n      function isComposerEditor(el) {\n        if (!(el instanceof Element)) return false\n        if (typeof el.closest !== 'function') return false\n        if (el.closest('[data-composer-card]') === null) return false\n        if (el instanceof HTMLTextAreaElement) return true\n        if (el.isContentEditable === true) return true\n        // jsdom / 无 isContentEditable 的实现兜底：Lexical 根节点是 contenteditable=\"true\"，\n        // 事件目标可能是其内部段落/文本容器，向上找一次即可。\n        return el.closest('[contenteditable=\"true\"]') !== null\n      }\n\n      function onKeyDown(e) {\n        if (e.key === 'Escape') {\n",
      "adaptedFor": "ab594842：上游 observer 注册行上下文变化（回调尾部改为 scheduleAssistantDecorate），find 改锚该尾部；注入的 rootObserver/bindMessageObserver 与 isComposerEditor 原样保留（isComposerEditor 仍被 op23 使用）。"
    },
    {
      "id": "op23-lexical-enter-guard",
      "group": "p0901-lexical-compat",
      "description": "PATCH(2026-09-01-lexical)：回车拼稿守卫改用 isComposerEditor()——旧守卫只认 HTMLTextAreaElement，宿主 composer Lexical 化后永不成立（引用不随消息发出、chip 常驻）",
      "find": "\n        if (e.key === 'Enter' && !e.shiftKey && !e.altKey\n          && ui.quotes.length > 0 && !isImeKeyBlocked(e)) {\n          var input = e.target instanceof Element && e.target.closest('[data-composer-input]')\n          if (input !== null && input.closest('[data-composer-card]') !== null) {\n            var attached = attachAndSend(e)\n            if (attached && (e.ctrlKey || e.metaKey)) {\n              e.preventDefault()\n",
      "replace": "\n        if (e.key === 'Enter' && !e.shiftKey && !e.altKey\n          && ui.quotes.length > 0 && !isImeKeyBlocked(e)) {\n          var ta = e.target\n          // PATCH(2026-09-01-lexical): 旧守卫只认 HTMLTextAreaElement → Lexical 下永不成立\n          if (isComposerEditor(ta)) {\n            var attached = attachAndSend(e)\n            if (attached && (e.ctrlKey || e.metaKey)) {\n              e.preventDefault()\n",
      "adaptedFor": "ab594842：上游把回车守卫改为 e.target.closest('[data-composer-input]')；fork 的 isComposerEditor 是超集（旧 textarea + 新 contenteditable），保留 fork 守卫并改锚上游新判定行（保持 PATCH 标记与两种输入面兼容）。"
    },
    {
      "id": "op25-lexical-chip-resize",
      "group": "p0901-lexical-compat",
      "description": "PATCH(2026-09-01-lexical)：chip ResizeObserver 观察 textarea 或 contenteditable 输入面",
      "find": "\n          // 可能收不到变化；一并观察这两个节点，兼容不同 DSH snapshot。\n          var inputScroll = card.querySelector('[data-input-scroll]')\n          if (inputScroll !== null) chipResizeObserver.observe(inputScroll)\n          var textarea = card.querySelector('textarea')\n          if (textarea !== null) chipResizeObserver.observe(textarea)\n        }\n      }\n\n",
      "replace": "\n          // 可能收不到变化；一并观察这两个节点，兼容不同 DSH snapshot。\n          var inputScroll = card.querySelector('[data-input-scroll]')\n          if (inputScroll !== null) chipResizeObserver.observe(inputScroll)\n          // PATCH(2026-09-01-lexical): 输入面是 textarea 或 contenteditable，两者都观察\n          var inputBox = card.querySelector('textarea')\n            || card.querySelector('[contenteditable=\"true\"]')\n          if (inputBox !== null) chipResizeObserver.observe(inputBox)\n        }\n      }\n\n",
      "adaptedFor": "ab594842：op25 的锚点位于 op15 注入的 observeChipComposer 内（textarea/contenteditable 观察行）；op15 适配恢复后该文本重新出现，op25 本身无需改锚（依赖顺序 op15 → op25）。"
    },
    {
      "id": "op26-flow-observer",
      "group": "p0827-flow-observer",
      "description": "补录 6c6dd18（2026-08-27）漏记改动：会话切换清空 pendingDeco / MutationObserver 收敛到消息流根节点 / 装饰轮询改有界 setTimeout（hunk 5）",
      "find": "\n        }\n      }\n\n      var decoTimer = null\n      function kickDecorate() {\n        decorateAll()\n        if (decoTimer === null) decoTimer = setInterval(decorateAll, 1000)\n      }\n\n      // ---------- locale 服务（zh/en）订阅与实时回流 ----------\n",
      "replace": "\n        }\n      }\n\n      // 发送/初次挂载后做一个最多 5 秒的有界兜底轮询；正常更新由消息流\n      // MutationObserver 驱动。这样仍覆盖异步 hydration，同时不再永久每秒全量扫描。\n      var decoTimer = null\n      var decoDeadline = 0\n      function scheduleDecoratePoll() {\n        if (decoTimer !== null) return\n        decoTimer = setTimeout(function tickDecorate() {\n          decoTimer = null\n          decorateAll()\n          if (Date.now() < decoDeadline) {\n            decoTimer = setTimeout(tickDecorate, 500)\n          }\n        }, 250)\n      }\n      function kickDecorate() {\n        decorateAll()\n        decoDeadline = Date.now() + 5000\n        scheduleDecoratePoll()\n      }\n\n      // ---------- locale 服务（zh/en）订阅与实时回流 ----------\n"
    },
    {
      "id": "op28-flow-observer",
      "group": "p0827-flow-observer",
      "description": "补录 6c6dd18（2026-08-27）漏记改动：会话切换清空 pendingDeco / MutationObserver 收敛到消息流根节点 / 装饰轮询改有界 setTimeout（hunk 7）",
      "find": "        chipComposerCard = null\n        host.removeEventListener('pointerdown', onHostPointerDown)\n        observer.disconnect()\n        if (typeof unsub === 'function') unsub()\n        if (inputWatchTimer !== null) { clearInterval(inputWatchTimer); inputWatchTimer = null }\n        if (typeof inputUnsub === 'function') inputUnsub()\n        if (typeof localeUnsub === 'function') localeUnsub()\n        if (decoTimer !== null) { clearInterval(decoTimer); decoTimer = null }\n        if (composerObserver !== null) composerObserver.disconnect()\n        chipLayer.remove()\n        tipLayer.remove()\n        host.remove()\n",
      "replace": "        chipComposerCard = null\n        host.removeEventListener('pointerdown', onHostPointerDown)\n        observer.disconnect()\n        rootObserver.disconnect()\n        observerTarget = null\n        if (typeof unsub === 'function') unsub()\n        if (inputWatchTimer !== null) { clearInterval(inputWatchTimer); inputWatchTimer = null }\n        if (typeof inputUnsub === 'function') inputUnsub()\n        if (typeof localeUnsub === 'function') localeUnsub()\n        if (decoTimer !== null) { clearTimeout(decoTimer); decoTimer = null }\n        decoDeadline = 0\n        chipLayer.remove()\n        tipLayer.remove()\n        host.remove()\n",
      "adaptedFor": "ab594842：上游 dispose 新增 inputWatchTimer 清理与 composerObserver.disconnect()；find 纳入前者，replace 用 rootObserver/observerTarget 收尾并移除 composerObserver 行（op15 已不再声明它），setTimeout 版本沿用 fork 的有界轮询清理。"
    },
    {
      "id": "op29-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出（根因）：showChipTip 卡片 cssText 加 box-sizing:border-box（tipLayer 在 body 下不带 [data-annotation-for-dsh]，默认 content-box，padding+border 使实际宽=标称+26px，钳位按标称算致右侧溢出）",
      "find": "\n        el.style.cssText = 'position:fixed;z-index:1160;width:300px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;",
      "replace": "\n        el.style.cssText = 'position:fixed;box-sizing:border-box;z-index:1160;width:300px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;"
    },
    {
      "id": "op30-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：气泡「引用 ×N」标签 tip cssText 加 box-sizing:border-box（同 op29 根因，12 空格缩进区分）",
      "find": "\n            el.style.cssText = 'position:fixed;z-index:1160;width:300px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;",
      "replace": "\n            el.style.cssText = 'position:fixed;box-sizing:border-box;z-index:1160;width:300px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;"
    },
    {
      "id": "op31-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：回复「Annotation N」芯片 tip（width:320px）cssText 加 box-sizing:border-box",
      "find": "\n          el.style.cssText = 'position:fixed;z-index:1160;width:320px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;",
      "replace": "\n          el.style.cssText = 'position:fixed;box-sizing:border-box;z-index:1160;width:320px;max-width:calc(100vw - 16px);padding:10px 12px;border-radius:12px;"
    },
    {
      "id": "op32-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：showChipTip 水平钳位改用实测外框宽 el.offsetWidth（任何盒模型下算术都对，双保险）",
      "find": "\n        var r2 = chipLayer.getBoundingClientRect()\n        var w2 = 300\n",
      "replace": "\n        var r2 = chipLayer.getBoundingClientRect()\n        var w2 = el.offsetWidth || 300\n"
    },
    {
      "id": "op33-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：气泡标签 tip 水平钳位改用 el.offsetWidth",
      "find": "\n            var r2 = tag.getBoundingClientRect()\n            var w2 = 300\n",
      "replace": "\n            var r2 = tag.getBoundingClientRect()\n            var w2 = el.offsetWidth || 300\n"
    },
    {
      "id": "op34-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：回复芯片 tip 水平钳位改用 el.offsetWidth",
      "find": "\n          var r2 = chip.getBoundingClientRect()\n          var w2 = 320\n",
      "replace": "\n          var r2 = chip.getBoundingClientRect()\n          var w2 = el.offsetWidth || 320\n"
    },
    {
      "id": "op35-p0903-tip-clamp",
      "group": "p0903-tip-clamp",
      "description": "P0903 悬浮卡右边溢出：tipLayer 创建处补根因注释（三处 tip 定位共用说明）",
      "find": "\n      var tipLayer = document.createElement('div')\n",
      "replace": "\n      // PATCH(2026-09-03): 悬浮卡右边溢出修复说明——三处 tip（showChipTip /\n      // 气泡「引用 ×N」标签 / 回复「Annotation N」芯片）都 append 到这个\n      // body 直下的 tipLayer；它不带 [data-annotation-for-dsh]，不继承\n      // border-box，content-box 下 padding+border 让实际宽比标称大 26px，\n      // 钳位按标称宽算 → 宽布局气泡贴右缘时卡片溢出屏幕右侧被裁。\n      // 对策：cssText 补 box-sizing:border-box + 钳位用 offsetWidth 实测。\n      var tipLayer = document.createElement('div')\n"
    },
    {
      "id": "op36-p0905-strip-think",
      "group": "p0905-strip-think",
      "description": "旧服务内联 think 兼容：定义 stripInlineThink（文本节点级 think/thinking/thought 块剥离，跨节点状态机）",
      "find": "      /** 扫描所有已结束流式输出的助手行：把「Annotation N：」替换为可悬浮芯片。 */",
      "replace": "      /** PATCH(2026-09-05): 旧模型服务把思考过程以 think / thinking /\n       *  thought 标签块内联进正文（没有独立 reasoning 通道），宿主按普通\n       *  文本渲染——用户看到大段独白，芯片替换还可能误命中独白里的\n       *  「Annotation N：」。停流后先把这些块从助手行文本节点剥掉；块被\n       *  markdown 拆成多个文本节点时，用状态机跨节点续接。\n       *  ponytail: 只认文本节点里的标签原文；若宿主渲染层把标签当 HTML\n       *  吞掉、只剩独白文字，则无从识别（属宿主渲染层问题）。 */\n      var THINK_OPEN_RE = /<\\s*(?:think(?:ing)?|thought|reasoning)\\s*>/i\n      var THINK_CLOSE_RE = /<\\s*\\/\\s*(?:think(?:ing)?|thought|reasoning)\\s*>/i\n      var THINK_BLOCK_RE = /<\\s*(?:think(?:ing)?|thought|reasoning)\\s*>[\\s\\S]*?<\\s*\\/\\s*(?:think(?:ing)?|thought|reasoning)\\s*>/gi\n      function stripInlineThink(row) {\n        var nodes = []\n        var walker = document.createTreeWalker(row, NodeFilter.SHOW_TEXT)\n        var n\n        while ((n = walker.nextNode()) !== null) nodes.push(n)\n        var inside = false\n        for (var i = 0; i < nodes.length; i++) {\n          n = nodes[i]\n          if (n.parentNode === null) continue\n          var v = n.nodeValue || ''\n          if (v === '') continue\n          if (!inside && v.indexOf('<') === -1) continue\n          THINK_BLOCK_RE.lastIndex = 0\n          var out = v.replace(THINK_BLOCK_RE, '')\n          if (!inside) {\n            var om = out.match(THINK_OPEN_RE)\n            if (om !== null) { out = out.slice(0, om.index); inside = true }\n          } else {\n            var cm = out.match(THINK_CLOSE_RE)\n            if (cm === null) out = ''\n            else { out = out.slice(cm.index + cm[0].length); inside = false }\n          }\n          if (out !== v) n.nodeValue = out\n        }\n      }\n\n      /** 扫描所有已结束流式输出的助手行：把「Annotation N：」替换为可悬浮芯片。 */"
    },
    {
      "id": "op37-p0905-strip-think",
      "group": "p0905-strip-think",
      "description": "旧服务内联 think 兼容：decorateAssistantAnnotations 停流守卫后先剥 think 块，再走芯片逻辑",
      "find": "          if (el.hasAttribute('data-streaming') || el.querySelector('[data-streaming]') !== null) continue\n          if (el.querySelector('[data-annotation-reply-chip]') !== null) continue",
      "replace": "          if (el.hasAttribute('data-streaming') || el.querySelector('[data-streaming]') !== null) continue\n          stripInlineThink(el) // PATCH(2026-09-05): 先剥内联 think 块，再做芯片替换\n          if (el.querySelector('[data-annotation-reply-chip]') !== null) continue"
    },
    {
      "id": "op38-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH(2026-09-17-current-session)：新增统一会话 id 解析器（旧快照 current → uiWorkspace 视图 → 持久化选择单元 → localStorage）",
      "find": "      var sessions = ctx.sessions\n",
      "replace": "      var sessions = ctx.sessions\n\n      // PATCH(2026-09-17-current-session): DSH 0.1.6 起 sessions 服务不再暴露「当前会话」，\n      // 线上 provider 已是 dsh-api-session-controller 的 ClientSessions（其 list 快照只有\n      // ids/byId/phase/subagentsByParent/jobsBySession，源码注释写明 view selection remains\n      // outside the Controller）。旧字段 .current 恒为 undefined 会让「回车拼稿 / 按会话落盘 /\n      // 发送后清 chip」三处一起静默失效（既无日志也无 toast），表现为引用发不出去且 chip 常驻。\n      // 这里做统一解析：① 老宿主快照 current；② 新宿主视图层保留的当前会话；③ uiWorkspace\n      // 持久化选择单元；④ 宿主自己写在 localStorage 的 dsh.sessions.current。\n      var sessionWarned = false\n      function currentSessionId() {\n        var snap = sessions.list.getSnapshot()\n        if (snap !== null && snap !== undefined && snap.current !== undefined) return snap.current\n        try {\n          var ui = typeof ctx.get === \"function\" ? ctx.get(\"uiWorkspace\") : undefined\n          if (ui !== null && ui !== undefined) {\n            var ref = ui.mainReference\n            if (ref !== null && ref !== undefined\n              && typeof ref.sessionId === \"string\" && ref.sessionId !== \"\") return ref.sessionId\n            var sel = ui.selection\n            if (sel !== null && sel !== undefined && typeof sel.getSnapshot === \"function\") {\n              var picked = sel.getSnapshot()\n              if (picked !== null && picked !== undefined\n                && typeof picked.sessionId === \"string\" && picked.sessionId !== \"\") return picked.sessionId\n            }\n          }\n        } catch (_) {}\n        try {\n          var raw = localStorage.getItem(\"dsh.sessions.current\")\n          if (typeof raw === \"string\" && raw !== \"\") {\n            var parsed = JSON.parse(raw)\n            if (parsed !== null && typeof parsed === \"object\"\n              && typeof parsed.sessionId === \"string\" && parsed.sessionId !== \"\"\n              && (snap === null || snap === undefined || !Array.isArray(snap.ids)\n                || snap.ids.indexOf(parsed.sessionId) !== -1)) return parsed.sessionId\n          }\n        } catch (_) {}\n        if (!sessionWarned) {\n          sessionWarned = true\n          console.warn(\"[annotation] 无法解析当前会话 id（宿主会话接口可能已变更）：引用拼稿与发送后清理不可用\")\n        }\n        return undefined\n      }\n"
    },
    {
      "id": "op39-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：writeCurrentPendingQuotes 改用统一解析器（此前 current 恒 undefined 导致待发送引用永不落盘）",
      "find": "      function writeCurrentPendingQuotes() {\n        writePendingQuotes(sessions.list.getSnapshot().current)\n      }\n",
      "replace": "      function writeCurrentPendingQuotes() {\n        writePendingQuotes(currentSessionId())\n      }\n"
    },
    {
      "id": "op40-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：选区来源判定改用统一解析器（1/2）",
      "find": "        if (source !== null && source.sessionId !== sessions.list.getSnapshot().current) rootEl = null\n",
      "replace": "        if (source !== null && source.sessionId !== currentSessionId()) rootEl = null\n"
    },
    {
      "id": "op41-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：选区来源判定改用统一解析器（2/2）",
      "find": "          if ((currentSource && currentSource.sourceUrl) !== (source && source.sourceUrl)\n            || (source !== null && source.sessionId !== sessions.list.getSnapshot().current)) return\n",
      "replace": "          if ((currentSource && currentSource.sourceUrl) !== (source && source.sourceUrl)\n            || (source !== null && source.sessionId !== currentSessionId())) return\n"
    },
    {
      "id": "op42-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：submitAttached 改用统一解析器（Ctrl/Cmd+Enter 纯引用直发）",
      "find": "      function submitAttached() {\n        var current = sessions.list.getSnapshot().current\n",
      "replace": "      function submitAttached() {\n        var current = currentSessionId()\n"
    },
    {
      "id": "op43-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：attachAndSend 改用统一解析器（回车拼稿入口——本次故障的直接断点）",
      "find": "      function attachAndSend(e) {\n        var current = sessions.list.getSnapshot().current\n",
      "replace": "      function attachAndSend(e) {\n        var current = currentSessionId()\n"
    },
    {
      "id": "op44-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：watchInputDraft 改用统一解析器（此前订阅永不建立，chip 永不清空）",
      "find": "        var id = sessions.list.getSnapshot().current\n        if (id !== undefined && tryWatchInputDraft(id)) return\n        inputWatchTimer = setInterval(function () {\n          var cur = sessions.list.getSnapshot().current\n",
      "replace": "        var id = currentSessionId()\n        if (id !== undefined && tryWatchInputDraft(id)) return\n        inputWatchTimer = setInterval(function () {\n          var cur = currentSessionId()\n"
    },
    {
      "id": "op45-p0917-current-session",
      "group": "p0917-current-session",
      "description": "PATCH：待发送引用按会话恢复/切换改用统一解析器",
      "find": "      var lastSessionId = sessions.list.getSnapshot().current\n      ui.quotes = readPendingQuotes(lastSessionId)\n      var unsub = sessions.list.subscribe(function () {\n        var cur = sessions.list.getSnapshot().current\n",
      "replace": "      var lastSessionId = currentSessionId()\n      ui.quotes = readPendingQuotes(lastSessionId)\n      var unsub = sessions.list.subscribe(function () {\n        var cur = currentSessionId()\n"
    },
    {
      "id": "op46-p0917-keep-question",
      "group": "p0917-keep-question",
      "description": "PATCH(2026-09-17-keep-question)：无标记纯引用块的补剥只对「以引用块首句开头」的草稿生效，且剥不动保持原样——修复用户自己输入的文字被整块吞掉（只发出纯引用）",
      "find": "          var clean = stripOldBlock(draft)\n          // ab594842：上游 v1.4.11 纯引用块走 block.headOnly/formatOnly（无「提问：」\n          // 标记），不在 stripOldBlock 的标记正则内；按首尾文案补剥，保留块后用户文字。\n          if (clean === draft) {\n            clean = draft.replace(\n              /^\\n*(?:我引用了以下|I annotated the following)[\\s\\S]*?(?:请按「Annotation N：…」的格式，逐条回应以上引用。|Please respond to each annotation above in the format \"Annotation N: …\".)\\n*/,\n              '',\n            )\n            // 仍剥不动（未知格式）：整块作废，避免旧块随新块重复发出。\n            if (clean === draft) clean = ''\n          }\n",
      "replace": "          var clean = stripOldBlock(draft)\n          // ab594842：上游 v1.4.11 纯引用块走 block.headOnly/formatOnly（无「提问：」\n          // 标记），不在 stripOldBlock 的标记正则内，需按首尾文案补剥。\n          // PATCH(2026-09-17-keep-question)：补剥只对「确实以引用块首句开头」的草稿生效，\n          // 且剥不动就保持原样。此前写法对任何草稿（包括只有用户自己输入、根本没有残留块\n          // 的正常情况）都会落到 else 分支把 clean 置空，结果是把用户输入整条吞掉，\n          // 只发出一条纯引用（0.3.1 用户实测：前端里自己打的字消失）。\n          if (clean === draft && /^\\n*(?:我引用了以下|I annotated the following)/.test(draft)) {\n            var peeled = draft.replace(\n              /^\\n*(?:我引用了以下|I annotated the following)[\\s\\S]*?(?:请按「Annotation N：…」的格式，逐条回应以上引用。|Please respond to each annotation above in the format \"Annotation N: …\".)\\n*/,\n              '',\n            )\n            if (peeled !== draft) clean = peeled\n          }\n"
    },
    {
      "id": "op47-p0918-edit-delete-i18n-zh",
      "description": "PATCH(2026-09-18-i18n)：zh 字典补 edit.delete（编辑卡「删除引用」按钮文案）",
      "find": "          save: '保存引用',\n        },\n",
      "replace": "          save: '保存引用',\n          delete: '删除引用',\n        },\n"
    },
    {
      "id": "op48-p0918-edit-delete-i18n-en",
      "description": "PATCH(2026-09-18-i18n)：en 字典补 edit.delete = Delete annotation",
      "find": "          save: 'Save annotation',\n        },\n",
      "replace": "          save: 'Save annotation',\n          delete: 'Delete annotation',\n        },\n"
    },
    {
      "id": "op49-p0918-edit-delete-i18n-use",
      "description": "PATCH(2026-09-18-i18n)：op08 注入的编辑态删除按钮改走 t('edit.delete')，不再硬编码中文",
      "find": "            del.textContent = '删除引用'\n",
      "replace": "            del.textContent = t('edit.delete')\n"
    }
  ],
  "adaptedFor": "2026-08-21 自 v1.3.13 升级重放至 fd24ef92；2026-09-01 补录 p0827-flow-observer + p0901-lexical-compat；2026-09-05 追加 p0905-strip-think；2026-09-17 基座迁移至 ab594842(v1.4.11-preview.1，适配 DSH 0.1.6)：11 条锚点适配（op00/op13/op15/op16/op17/op20/op21/op22/op23/op28），4 条退休（op04/op18/op24/op27，上游已覆盖，见 retired）；2026-09-18 追加 op47-op49（编辑卡「删除引用」走 t() 双语字典）",
  "retired": [
    {
      "id": "op04-p0814d-send-button",
      "description": "PATCH(2026-08-14d)：点击发送按钮同样拼稿（capture 拦截主发送按钮），修复点按钮发送丢引用",
      "retiredReason": "上游 v1.4.11-preview.1 自己实现了「点击/指针发送也拼稿」，且比 fork 更完整（含空草稿禁用按钮的直接提交路径），fork 的 onDocClickCapture 再注入会与之重复。",
      "upstreamEvidence": "新基座 lines 1226-1266：submitAttached()（1226-1236）、sendButtonOf()（1238-1245）、onSendPointerDown()（1249-1258，pointerdown capture；wasDisabled 时 preventDefault+submitAttached）、onSendKeyboardClick()（1261-1264，detail=0 键盘激活），并注册 document pointerdown/click capture（1265-1266）。fork 旧实现只在 click 且按钮 enabled 时拼稿，是上游行为的子集。"
    },
    {
      "id": "op18-p0814d-send-button",
      "description": "PATCH(2026-08-14d)：onDocClickCapture 清理（dispose 对称移除 click 监听）",
      "retiredReason": "随 op04 一并退休：onDocClickCapture 不再注入，若保留会在 dispose 里 removeEventListener 一个不存在的函数（引用未定义标识符）。上游 dispose 自行移除 onSendPointerDown/onSendKeyboardClick。",
      "upstreamEvidence": "新基座 lines 2320-2344 的清理函数，其中 2325-2326 为 document.removeEventListener('pointerdown', onSendPointerDown, true) 与 document.removeEventListener('click', onSendKeyboardClick, true)。"
    },
    {
      "id": "op24-lexical-focus-composer",
      "description": "PATCH(2026-09-01-lexical)：focusComposer 兼容 contenteditable 输入面（textarea 走 setSelectionRange，contenteditable 用 Range 折叠到文末）",
      "retiredReason": "上游 v1.4.11-preview.1 已把 focusComposer 改写成面向 Lexical contenteditable 的实现，语义等价：查 [data-composer-input]、isContentEditable 守卫、focus({preventScroll:true})、选区折叠到文末。",
      "upstreamEvidence": "新基座 lines 1646-1658：var input = document.querySelector('[data-composer-card] [data-composer-input]')；if (!(input instanceof HTMLElement) || !input.isContentEditable) return；rAF 内 input.focus({preventScroll:true}) + selection.selectAllChildren(input) + selection.collapseToEnd()。"
    },
    {
      "id": "op27-flow-observer",
      "description": "补录 6c6dd18：会话切换清空 pendingDeco（hunk 6）",
      "retiredReason": "上游 v1.4.11-preview.1 的会话切换订阅里已自行清空 pendingDeco，且改用 pendingDeco.length = 0（等价且保留数组引用），fork 的 pendingDeco = [] 无额外语义。",
      "upstreamEvidence": "新基座 lines 2297-2313 会话订阅：2303 ui.quotes = readPendingQuotes(cur)；2305-2307 注释「旧会话未消费的发送暂存作废」+ pendingDeco.length = 0。"
    }
  ]
}
